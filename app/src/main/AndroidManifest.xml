<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffeshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".BlomMeadworksActivity" />
        <activity android:name=".AvalonCoffeeActivity" />
        <activity android:name=".CometCoffeeActivity" />
        <activity android:name=".BlackDieselActivity" />
        <activity android:name=".LiteratiActivity" />
        <activity android:name=".VertexActivity" />
        <activity android:name=".EspressoRoyaleActivity" />
        <activity android:name=".RoosRoastActivity" />
        <activity android:name=".CheckInActivity" />
        <activity android:name=".AddPaymentMethodActivity" />
        <activity android:name=".MainPaymentActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".CoffeeShopActivity" />
        <activity android:name=".HomeActivity"/>
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>